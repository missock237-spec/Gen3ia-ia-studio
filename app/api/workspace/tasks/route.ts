import { NextRequest,NextResponse } from "next/server"; import { z } from "zod"; import { requireUser } from "@/lib/security/authenticated-request"; import { createWorkspaceTask } from "@/lib/agents/workspace";
const Schema=z.object({objective:z.string().min(3).max(20000)});
export async function POST(request:NextRequest){try{const user=await requireUser(request);const body=Schema.parse(await request.json());return NextResponse.json({success:true,task:await createWorkspaceTask(user.uid,body.objective)},{status:201});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Task creation failed"},{status:400});}}
